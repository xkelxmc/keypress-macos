// Default: ad-hoc signing so the generated Xcode project builds locally
// without certificates. Scripts/build_appstore.sh overwrites this file with
// the real team / profile for App Store builds (and restores it afterwards).
CODE_SIGN_STYLE = Manual
CODE_SIGN_IDENTITY = -
